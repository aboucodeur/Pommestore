/**
 * * Gestion des erreurs de l'application...
 * 
 * - Generation d'erreur cibler en creant une execption
 *   Custom error : if (!state.error) throw new AuthRequiredError();
 * 
 *  - Generation d'erreur UI
 *    Directement lier a l'application (not-found.tsx) !
 */